PORT=4000
# Path to the SQLite database file. On Railway, point this at a mounted volume
# (e.g. /data/dominant.db) so data survives redeploys.
DATABASE_PATH=./data/dominant.db
JWT_SECRET=change-this-to-a-long-random-string
